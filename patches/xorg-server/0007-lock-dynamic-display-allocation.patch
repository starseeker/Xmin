--- a/third_party/xorg-server/include/os.h
+++ b/third_party/xorg-server/include/os.h
@@ -301,6 +301,8 @@
 
 extern _X_EXPORT void
 LockServer(void);
+extern _X_EXPORT Bool
+TryLockServer(void);
 extern _X_EXPORT void
 UnlockServer(void);
 
--- a/third_party/xorg-server/os/osinit.c
+++ b/third_party/xorg-server/os/osinit.c
@@ -310,7 +310,10 @@
             }
         }
 #endif
-        LockServer();
+        /* A dynamic -displayfd server reserves its display while probing the
+         * candidate socket in CreateWellKnownSockets(). */
+        if (displayfd < 0 || explicit_display)
+            LockServer();
         been_here = TRUE;
     }
     TimerInit();
--- a/third_party/xorg-server/os/utils.c
+++ b/third_party/xorg-server/os/utils.c
@@ -248,11 +248,18 @@
 LockServer(void)
 {}
 
+Bool
+TryLockServer(void)
+{
+    return TRUE;
+}
+
 void
 UnlockServer(void)
 {}
 #else /* LOCK_SERVER */
 static Bool StillLocking = FALSE;
+static Bool HasLock = FALSE;
 static char LockFile[PATH_MAX];
 static Bool nolock = FALSE;
 
@@ -262,8 +269,8 @@
  *      contained inside is valid.  If so, then die.  Otherwise, create
  *      the lock file containing the PID.
  */
-void
-LockServer(void)
+static Bool
+LockServerInternal(Bool fatal)
 {
     char tmp[PATH_MAX], pid_str[12];
     int lfd, i, haslock, l_pid, t;
@@ -272,47 +279,31 @@
     char port[20];
 
     if (nolock || NoListenAll)
-        return;
+        return TRUE;
     /*
      * Path names
      */
     snprintf(port, sizeof(port), "%d", atoi(display));
     len = strlen(LOCK_PREFIX) > strlen(LOCK_TMP_PREFIX) ? strlen(LOCK_PREFIX) :
         strlen(LOCK_TMP_PREFIX);
-    len += strlen(tmppath) + strlen(port) + strlen(LOCK_SUFFIX) + 1;
+    len += strlen(tmppath) + strlen(port) + strlen(LOCK_SUFFIX) +
+        1 + 20 + 1;
     if (len > sizeof(LockFile))
         FatalError("Display name `%s' is too long\n", port);
-    (void) sprintf(tmp, "%s" LOCK_TMP_PREFIX "%s" LOCK_SUFFIX, tmppath, port);
+    (void) snprintf(tmp, sizeof(tmp),
+                    "%s" LOCK_TMP_PREFIX "%s" LOCK_SUFFIX ".%lu",
+                    tmppath, port, (unsigned long) getpid());
     (void) sprintf(LockFile, "%s" LOCK_PREFIX "%s" LOCK_SUFFIX, tmppath, port);
 
-    /*
-     * Create a temporary file containing our PID.  Attempt three times
-     * to create the file.
-     */
+    /* Create a per-process temporary file containing our PID. */
     StillLocking = TRUE;
-    i = 0;
-    do {
-        i++;
-        lfd = open(tmp, O_CREAT | O_EXCL | O_WRONLY, 0644);
-        if (lfd < 0)
-            sleep(2);
-        else
-            break;
-    } while (i < 3);
+    lfd = open(tmp, O_CREAT | O_EXCL | O_WRONLY, 0644);
     if (lfd < 0) {
-        unlink(tmp);
-        i = 0;
-        do {
-            i++;
-            lfd = open(tmp, O_CREAT | O_EXCL | O_WRONLY, 0644);
-            if (lfd < 0)
-                sleep(2);
-            else
-                break;
-        } while (i < 3);
+        if (fatal)
+            FatalError("Could not create lock file in %s\n", tmp);
+        StillLocking = FALSE;
+        return FALSE;
     }
-    if (lfd < 0)
-        FatalError("Could not create lock file in %s\n", tmp);
     snprintf(pid_str, sizeof(pid_str), "%10lu\n", (unsigned long) getpid());
     if (write(lfd, pid_str, 11) != 11)
         FatalError("Could not write pid to lock file in %s\n", tmp);
@@ -340,7 +331,10 @@
             lfd = open(LockFile, O_RDONLY | O_NOFOLLOW);
             if (lfd < 0) {
                 unlink(tmp);
-                FatalError("Can't read lock file %s\n", LockFile);
+                if (fatal)
+                    FatalError("Can't read lock file %s\n", LockFile);
+                StillLocking = FALSE;
+                return FALSE;
             }
             pid_str[0] = '\0';
             if (read(lfd, pid_str, 11) != 11) {
@@ -372,23 +366,47 @@
                  * Process is still active.
                  */
                 unlink(tmp);
-                FatalError
-                    ("Server is already active for display %s\n%s %s\n%s\n",
-                     port, "\tIf this server is no longer running, remove",
-                     LockFile, "\tand start again.");
+                if (fatal)
+                    FatalError
+                        ("Server is already active for display %s\n%s %s\n%s\n",
+                         port, "\tIf this server is no longer running, remove",
+                         LockFile, "\tand start again.");
+                StillLocking = FALSE;
+                return FALSE;
             }
         }
         else {
             unlink(tmp);
-            FatalError
-                ("Linking lock file (%s) in place failed: %s\n",
-                 LockFile, strerror(errno));
+            if (fatal)
+                FatalError
+                    ("Linking lock file (%s) in place failed: %s\n",
+                     LockFile, strerror(errno));
+            StillLocking = FALSE;
+            return FALSE;
         }
     }
     unlink(tmp);
-    if (!haslock)
-        FatalError("Could not create server lock file: %s\n", LockFile);
+    if (!haslock) {
+        if (fatal)
+            FatalError("Could not create server lock file: %s\n", LockFile);
+        StillLocking = FALSE;
+        return FALSE;
+    }
     StillLocking = FALSE;
+    HasLock = TRUE;
+    return TRUE;
+}
+
+void
+LockServer(void)
+{
+    (void) LockServerInternal(TRUE);
+}
+
+Bool
+TryLockServer(void)
+{
+    return LockServerInternal(FALSE);
 }
 
 /*
@@ -400,9 +418,9 @@
     if (nolock || NoListenAll)
         return;
 
-    if (!StillLocking) {
-
+    if (!StillLocking && HasLock) {
         (void) unlink(LockFile);
+        HasLock = FALSE;
     }
 }
 #endif /* LOCK_SERVER */
@@ -771,9 +789,6 @@
         else if (strcmp(argv[i], "-displayfd") == 0) {
             if (++i < argc) {
                 displayfd = atoi(argv[i]);
-#ifdef LOCK_SERVER
-                nolock = TRUE;
-#endif
             }
             else
                 UseMsg();
--- a/third_party/xorg-server/os/connection.c
+++ b/third_party/xorg-server/os/connection.c
@@ -259,17 +259,19 @@
     else { /* -displayfd and no explicit display number */
         Bool found = 0;
         for (i = 0; i < 65536 - X_TCP_PORT; i++) {
+            snprintf(dynamic_display, sizeof(dynamic_display), "%d", i);
+            display = dynamic_display;
+            if (!TryLockServer())
+                continue;
             if (TryCreateSocket(i, &partial) && !partial) {
                 found = 1;
                 break;
             }
-            else
-                CloseWellKnownConnections();
+            CloseWellKnownConnections();
+            UnlockServer();
         }
         if (!found)
             FatalError("Failed to find a socket to listen on");
-        snprintf(dynamic_display, sizeof(dynamic_display), "%d", i);
-        display = dynamic_display;
         LogSetDisplay();
     }
 
