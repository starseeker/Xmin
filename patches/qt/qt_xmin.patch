diff -Naur qtbase/src/gui/CMakeLists.txt Qt6_BLD/src/gui/CMakeLists.txt
--- qtbase/src/gui/CMakeLists.txt	2026-07-16 10:33:44.350789000 +0000
+++ Qt6_BLD/src/gui/CMakeLists.txt	2026-07-16 12:21:40.435797000 +0000
@@ -373,6 +373,8 @@
                     PlatformGraphics::PlatformGraphics
             )
         endif()
+    elseif(QT_FEATURE_xmin_opengl)
+        target_link_libraries(Gui PUBLIC Xmin::GL)
     elseif(NOT QT_FEATURE_opengl_dynamic)
         target_link_libraries(Gui PUBLIC WrapOpenGL::WrapOpenGL)
     endif()
@@ -1087,14 +1089,20 @@
         platform/unix/dbustray/qxdgnotificationproxy.cpp platform/unix/dbustray/qxdgnotificationproxy_p.h
 )
 
+if(QT_FEATURE_xmin_x11)
+    set(qt_xkbcommon_target Xmin::QtX11)
+else()
+    set(qt_xkbcommon_target XKB::XKB)
+endif()
+
 qt_internal_extend_target(Gui CONDITION QT_FEATURE_xkbcommon AND UNIX
     SOURCES
         platform/unix/qxkbcommon.cpp platform/unix/qxkbcommon_p.h
         platform/unix/qxkbcommon_3rdparty.cpp
     LIBRARIES
-        XKB::XKB
+        ${qt_xkbcommon_target}
     PRIVATE_MODULE_INTERFACE
-        XKB::XKB
+        ${qt_xkbcommon_target}
 )
 
 qt_internal_extend_target(Gui CONDITION QT_FEATURE_metal
diff -Naur qtbase/src/gui/configure.cmake Qt6_BLD/src/gui/configure.cmake
--- qtbase/src/gui/configure.cmake	2026-07-16 10:33:44.355056000 +0000
+++ Qt6_BLD/src/gui/configure.cmake	2026-07-16 12:21:40.435481000 +0000
@@ -94,9 +94,11 @@
     qt_add_qmake_lib_dependency(libpng zlib)
 endif()
 qt_find_package(Mtdev MODULE PROVIDED_TARGETS PkgConfig::Mtdev MODULE_NAME gui QMAKE_LIB mtdev)
+if(NOT FEATURE_xmin_x11)
 qt_find_package(WrapOpenGL MODULE PROVIDED_TARGETS WrapOpenGL::WrapOpenGL MODULE_NAME gui QMAKE_LIB opengl)
 qt_find_package(GLESv2 MODULE
     PROVIDED_TARGETS GLESv2::GLESv2 MODULE_NAME gui QMAKE_LIB opengl_es2)
+endif()
 qt_find_package(Tslib MODULE PROVIDED_TARGETS PkgConfig::Tslib MODULE_NAME gui QMAKE_LIB tslib)
 qt_find_package(WrapVulkanHeaders MODULE PROVIDED_TARGETS WrapVulkanHeaders::WrapVulkanHeaders
     MODULE_NAME gui QMAKE_LIB vulkan MARK_OPTIONAL)
@@ -115,6 +117,16 @@
     qt_find_package(Wayland MODULE PROVIDED_TARGETS Wayland::Egl
                     MODULE_NAME gui QMAKE_LIB wayland_egl)
 endif()
+if(FEATURE_xmin_x11)
+    qt_find_package(Xmin 0.1 CONFIG
+        PROVIDED_TARGETS Xmin::QtX11 Xmin::GL MODULE_NAME gui QMAKE_LIB xmin_x11)
+    if(TARGET Xmin::GL AND NOT TARGET WrapOpenGL::WrapOpenGL)
+        add_library(WrapOpenGL::WrapOpenGL INTERFACE IMPORTED)
+        set_property(TARGET WrapOpenGL::WrapOpenGL PROPERTY
+            INTERFACE_LINK_LIBRARIES Xmin::GL)
+        set(WrapOpenGL_FOUND ON)
+    endif()
+else()
 if((X11_SUPPORTED) OR QT_FIND_ALL_PACKAGES_ALWAYS)
     qt_find_package(X11 MODULE PROVIDED_TARGETS X11::X11 MODULE_NAME gui QMAKE_LIB xlib)
 endif()
@@ -215,6 +227,7 @@
         PROVIDED_TARGETS PkgConfig::XRender MODULE_NAME gui QMAKE_LIB xrender)
 endif()
 qt_add_qmake_lib_dependency(xrender xlib)
+endif()
 
 # qt wayland client
 if(LINUX OR FREEBSD OR QT_FIND_ALL_PACKAGES_ALWAYS)
@@ -660,6 +673,53 @@
 }
 ")
 
+if(FEATURE_xmin_x11)
+    # Xmin supplies the complete raster qxcb/xkbcommon client closure as one
+    # package target. Keep this separate from conventional X stack discovery
+    # so an opt-in minimal build cannot silently acquire host X libraries.
+    qt_config_compile_test(xmin_x11
+        LABEL "Xmin X11 client"
+        LIBRARIES
+            Xmin::QtX11
+        CODE
+"#define explicit dont_use_cxx_explicit
+#include <xcb/xcb.h>
+#include <xcb/xcb_cursor.h>
+#include <xcb/xcb_icccm.h>
+#include <xcb/xcb_image.h>
+#include <xcb/xcb_keysyms.h>
+#include <xcb/xcb_renderutil.h>
+#include <xcb/randr.h>
+#include <xcb/render.h>
+#include <xcb/shape.h>
+#include <xcb/shm.h>
+#include <xcb/sync.h>
+#include <xcb/xfixes.h>
+#include <xcb/xkb.h>
+#undef explicit
+#include <xkbcommon/xkbcommon.h>
+#include <xkbcommon/xkbcommon-x11.h>
+
+int main(void)
+{
+    int screen = 0;
+    xcb_connection_t *connection = xcb_connect(nullptr, &screen);
+    xcb_render_query_pict_formats(connection);
+    xcb_render_util_find_standard_format(nullptr, XCB_PICT_STANDARD_ARGB_32);
+    xkb_context *context = xkb_context_new(XKB_CONTEXT_NO_DEFAULT_INCLUDES);
+    xkb_x11_setup_xkb_extension(
+        connection,
+        XKB_X11_MIN_MAJOR_XKB_VERSION,
+        XKB_X11_MIN_MINOR_XKB_VERSION,
+        XKB_X11_SETUP_XKB_EXTENSION_NO_FLAGS,
+        nullptr, nullptr, nullptr, nullptr);
+    xkb_context_unref(context);
+    xcb_disconnect(connection);
+    return 0;
+}
+")
+endif()
+
 # libinput_hires_wheel_support
 qt_config_compile_test(libinput_hires_wheel_support
     LABEL "libinput hires wheel support"
@@ -908,6 +968,7 @@
 
 #### Features
 
+
 qt_feature("accessibility-atspi-bridge" PUBLIC PRIVATE
     LABEL "ATSPI Bridge"
     CONDITION QT_FEATURE_accessibility AND QT_FEATURE_dbus AND ATSPI2_FOUND
@@ -1221,17 +1282,27 @@
     PURPOSE "Provides the TuioTouch input plugin."
     CONDITION QT_FEATURE_network AND QT_FEATURE_udpsocket
 )
+qt_feature("xmin-x11" PRIVATE
+    SECTION "Platform plugins"
+    LABEL "Xmin minimalist X11 client"
+    CONDITION TARGET Xmin::QtX11 AND TEST_xmin_x11
+)
 qt_feature("xcb" PUBLIC
     SECTION "Platform plugins"
     LABEL "XCB"
     AUTODETECT NOT APPLE
-    CONDITION QT_FEATURE_thread AND TARGET XCB::XCB AND TEST_xcb_syslibs AND QT_FEATURE_xkbcommon_x11
+    CONDITION QT_FEATURE_thread AND ((TARGET XCB::XCB AND TEST_xcb_syslibs AND QT_FEATURE_xkbcommon_x11) OR (QT_FEATURE_xmin_x11 AND TEST_xmin_x11))
 )
 qt_feature("xcb-glx-plugin" PUBLIC
     LABEL "GLX Plugin"
     CONDITION QT_FEATURE_xcb_xlib AND QT_FEATURE_opengl AND NOT QT_FEATURE_opengles2
     EMIT_IF QT_FEATURE_xcb
 )
+qt_feature("xmin-opengl" PRIVATE
+    LABEL "Xmin software-direct OpenGL"
+    CONDITION QT_FEATURE_xmin_x11 AND TARGET Xmin::GL AND QT_FEATURE_opengl
+    EMIT_IF QT_FEATURE_xcb
+)
 qt_feature("xcb-glx" PRIVATE
     LABEL "  XCB GLX"
     CONDITION XCB_GLX_FOUND
@@ -1261,11 +1332,11 @@
 )
 qt_feature("xkbcommon" PUBLIC
     LABEL "xkbcommon"
-    CONDITION XKB_FOUND
+    CONDITION XKB_FOUND OR QT_FEATURE_xmin_x11
 )
 qt_feature("xkbcommon-x11" PRIVATE
     LABEL "xkbcommon-x11"
-    CONDITION QT_FEATURE_xkbcommon AND XKB_COMMON_X11_FOUND
+    CONDITION QT_FEATURE_xkbcommon AND (XKB_COMMON_X11_FOUND OR QT_FEATURE_xmin_x11)
 )
 qt_feature("xlib" PRIVATE
     LABEL "XLib"
@@ -1800,4 +1871,3 @@
 
 
 #### Features
-
diff -Naur qtbase/src/gui/qt_cmdline.cmake Qt6_BLD/src/gui/qt_cmdline.cmake
--- qtbase/src/gui/qt_cmdline.cmake	2026-07-16 10:33:44.448650000 +0000
+++ Qt6_BLD/src/gui/qt_cmdline.cmake	2026-07-16 11:32:09.170020000 +0000
@@ -38,6 +38,7 @@
 qt_commandline_option(tslib TYPE boolean)
 qt_commandline_option(vulkan TYPE boolean)
 qt_commandline_option(xcb TYPE boolean)
+qt_commandline_option(xmin-x11 TYPE boolean)
 qt_commandline_option(bundled-xcb-xinput TYPE boolean)
 qt_commandline_option(xcb-native-painting TYPE boolean)
 qt_commandline_option(xcb-xlib TYPE boolean)
diff -Naur qtbase/src/plugins/platforminputcontexts/compose/CMakeLists.txt Qt6_BLD/src/plugins/platforminputcontexts/compose/CMakeLists.txt
--- qtbase/src/plugins/platforminputcontexts/compose/CMakeLists.txt	2026-07-16 10:33:44.577609000 +0000
+++ Qt6_BLD/src/plugins/platforminputcontexts/compose/CMakeLists.txt	2026-07-16 11:32:45.724432000 +0000
@@ -5,8 +5,14 @@
 ## QComposePlatformInputContextPlugin Plugin:
 #####################################################################
 
-qt_find_package(XKB MODULE)
-pkg_get_variable(PKG_X11_PREFIX x11 prefix)
+if(QT_FEATURE_xmin_x11)
+    set(qt_compose_xkb_target Xmin::QtX11)
+    set(PKG_X11_PREFIX "")
+else()
+    qt_find_package(XKB MODULE)
+    pkg_get_variable(PKG_X11_PREFIX x11 prefix)
+    set(qt_compose_xkb_target XKB::XKB)
+endif()
 
 qt_internal_add_plugin(QComposePlatformInputContextPlugin
     OUTPUT_NAME composeplatforminputcontextplugin
@@ -20,5 +26,5 @@
         Qt::CorePrivate
         Qt::Gui
         Qt::GuiPrivate
-        XKB::XKB
+        ${qt_compose_xkb_target}
 )
diff -Naur qtbase/src/plugins/platforms/xcb/CMakeLists.txt Qt6_BLD/src/plugins/platforms/xcb/CMakeLists.txt
--- qtbase/src/plugins/platforms/xcb/CMakeLists.txt	2026-07-16 10:33:44.653796000 +0000
+++ Qt6_BLD/src/plugins/platforms/xcb/CMakeLists.txt	2026-07-16 12:21:40.435976000 +0000
@@ -10,6 +10,29 @@
     add_compile_options(-Wno-psabi)
 endif()
 
+if(QT_FEATURE_xmin_x11)
+    set(qt_xcb_client_libraries Xmin::QtX11)
+else()
+    set(qt_xcb_client_libraries
+        PkgConfig::XKB_COMMON_X11
+        XCB::CURSOR
+        XCB::ICCCM
+        XCB::UTIL
+        XCB::IMAGE
+        XCB::KEYSYMS
+        XCB::RANDR
+        XCB::RENDER
+        XCB::RENDERUTIL
+        XCB::SHAPE
+        XCB::SHM
+        XCB::SYNC
+        XCB::XCB
+        XCB::XFIXES
+        XCB::XKB
+        XKB::XKB
+    )
+endif()
+
 qt_internal_add_module(XcbQpaPrivate
     CONFIG_MODULE_NAME xcb_qpa_lib
     INTERNAL_MODULE
@@ -47,24 +70,9 @@
     INCLUDE_DIRECTORIES
         gl_integrations
     PUBLIC_LIBRARIES
-        PkgConfig::XKB_COMMON_X11
         Qt::CorePrivate
         Qt::GuiPrivate
-        XCB::CURSOR
-        XCB::ICCCM
-        XCB::UTIL
-        XCB::IMAGE
-        XCB::KEYSYMS
-        XCB::RANDR
-        XCB::RENDER
-        XCB::RENDERUTIL
-        XCB::SHAPE
-        XCB::SHM
-        XCB::SYNC
-        XCB::XCB
-        XCB::XFIXES
-        XCB::XKB
-        XKB::XKB
+        ${qt_xcb_client_libraries}
     NO_UNITY_BUILD # X11 define clashes
 )
 
@@ -89,6 +97,11 @@
     PUBLIC_LIBRARIES
         X11::XCB
         X11::X11
+)
+
+qt_internal_extend_target(XcbQpaPrivate CONDITION QT_FEATURE_xmin_opengl
+    DEFINES
+        QT_XCB_XMIN_OPENGL
 )
 
 qt_internal_extend_target(XcbQpaPrivate CONDITION NOT QT_FEATURE_xcb_xlib
diff -Naur qtbase/src/plugins/platforms/xcb/gl_integrations/CMakeLists.txt Qt6_BLD/src/plugins/platforms/xcb/gl_integrations/CMakeLists.txt
--- qtbase/src/plugins/platforms/xcb/gl_integrations/CMakeLists.txt	2026-07-16 10:33:44.653885000 +0000
+++ Qt6_BLD/src/plugins/platforms/xcb/gl_integrations/CMakeLists.txt	2026-07-16 12:21:40.436466000 +0000
@@ -7,3 +7,6 @@
 if(QT_FEATURE_xcb_glx_plugin)
     add_subdirectory(xcb_glx)
 endif()
+if(QT_FEATURE_xmin_opengl)
+    add_subdirectory(xcb_xmin)
+endif()
diff -Naur qtbase/src/plugins/platforms/xcb/gl_integrations/xcb_xmin/CMakeLists.txt Qt6_BLD/src/plugins/platforms/xcb/gl_integrations/xcb_xmin/CMakeLists.txt
--- qtbase/src/plugins/platforms/xcb/gl_integrations/xcb_xmin/CMakeLists.txt	1970-01-01 00:00:00.000000000 +0000
+++ Qt6_BLD/src/plugins/platforms/xcb/gl_integrations/xcb_xmin/CMakeLists.txt	2026-07-16 12:21:40.436623000 +0000
@@ -0,0 +1,21 @@
+# Copyright (C) 2026 Xmin Project
+# SPDX-License-Identifier: BSD-3-Clause
+
+qt_internal_add_plugin(QXcbXminIntegrationPlugin
+    OUTPUT_NAME qxcb-xmin-integration
+    PLUGIN_TYPE xcbglintegrations
+    SOURCES
+        qxcbxmincontext.cpp qxcbxmincontext.h
+        qxcbxminintegration.cpp qxcbxminintegration.h
+        qxcbxminmain.cpp
+    INCLUDE_DIRECTORIES
+        ..
+        ../..
+    LIBRARIES
+        Qt::Core
+        Qt::CorePrivate
+        Qt::Gui
+        Qt::GuiPrivate
+        Qt::XcbQpaPrivate
+        Xmin::GL
+)
diff -Naur qtbase/src/plugins/platforms/xcb/gl_integrations/xcb_xmin/qxcbxmincontext.cpp Qt6_BLD/src/plugins/platforms/xcb/gl_integrations/xcb_xmin/qxcbxmincontext.cpp
--- qtbase/src/plugins/platforms/xcb/gl_integrations/xcb_xmin/qxcbxmincontext.cpp	1970-01-01 00:00:00.000000000 +0000
+++ Qt6_BLD/src/plugins/platforms/xcb/gl_integrations/xcb_xmin/qxcbxmincontext.cpp	2026-07-16 12:21:40.436867000 +0000
@@ -0,0 +1,108 @@
+// Copyright (C) 2026 Xmin Project
+// SPDX-License-Identifier: LicenseRef-Qt-Commercial OR LGPL-3.0-only OR GPL-2.0-only OR GPL-3.0-only
+
+#include "qxcbxmincontext.h"
+
+#include "qxcbconnection.h"
+#include "qxcbscreen.h"
+#include "qxcbwindow.h"
+
+#include <QtGui/QOpenGLContext>
+#include <Xmin/GLXxcb.h>
+
+#include <cstdlib>
+
+QT_BEGIN_NAMESPACE
+
+QXcbXminContext::QXcbXminContext(QXcbConnection *connection,
+                                 QOpenGLContext *context)
+{
+    QXcbScreen *screen = static_cast<QXcbScreen *>(context->screen()->handle());
+    m_format = screen->surfaceFormatFor(context->format());
+    m_format.setRenderableType(QSurfaceFormat::OpenGL);
+    m_format.setVersion(2, 0);
+    m_format.setProfile(QSurfaceFormat::NoProfile);
+
+    m_display = xminGlxCreateXcbDisplay(connection->xcb_connection());
+    if (!m_display)
+        return;
+
+    if (context->shareHandle()) {
+        const auto *share = static_cast<const QXcbXminContext *>(context->shareHandle());
+        m_shareContext = share->m_context;
+    }
+
+    const int attributes[] = {
+        GLX_DRAWABLE_TYPE, GLX_WINDOW_BIT,
+        GLX_RENDER_TYPE, GLX_RGBA_BIT,
+        GLX_DOUBLEBUFFER,
+            m_format.swapBehavior() == QSurfaceFormat::SingleBuffer ? False : True,
+        GLX_RED_SIZE, qMax(1, m_format.redBufferSize()),
+        GLX_GREEN_SIZE, qMax(1, m_format.greenBufferSize()),
+        GLX_BLUE_SIZE, qMax(1, m_format.blueBufferSize()),
+        GLX_ALPHA_SIZE, qMax(0, m_format.alphaBufferSize()),
+        GLX_DEPTH_SIZE, qMax(0, m_format.depthBufferSize()),
+        GLX_STENCIL_SIZE, qMax(0, m_format.stencilBufferSize()),
+        0
+    };
+    int count = 0;
+    GLXFBConfig *configs = glXChooseFBConfig(m_display, screen->screenNumber(),
+                                              attributes, &count);
+    if (configs && count > 0) {
+        const int contextAttributes[] = {
+            GLX_CONTEXT_MAJOR_VERSION_ARB, 2,
+            GLX_CONTEXT_MINOR_VERSION_ARB, 0,
+            0
+        };
+        m_context = glXCreateContextAttribsARB(m_display, configs[0],
+                                                m_shareContext, True,
+                                                contextAttributes);
+        if (!m_context && m_shareContext) {
+            m_shareContext = nullptr;
+            m_context = glXCreateContextAttribsARB(m_display, configs[0],
+                                                    nullptr, True,
+                                                    contextAttributes);
+        }
+    }
+    std::free(configs);
+}
+
+QXcbXminContext::~QXcbXminContext()
+{
+    if (m_context)
+        glXDestroyContext(m_display, m_context);
+    xminGlxDestroyXcbDisplay(m_display);
+}
+
+bool QXcbXminContext::makeCurrent(QPlatformSurface *surface)
+{
+    if (!m_context || !surface ||
+        surface->surface()->surfaceClass() != QSurface::Window)
+        return false;
+    auto *window = static_cast<QXcbWindow *>(surface);
+    return glXMakeCurrent(m_display, window->xcb_window(), m_context);
+}
+
+void QXcbXminContext::doneCurrent()
+{
+    if (m_display)
+        glXMakeCurrent(m_display, 0, nullptr);
+}
+
+void QXcbXminContext::swapBuffers(QPlatformSurface *surface)
+{
+    if (!surface || surface->surface()->surfaceClass() != QSurface::Window)
+        return;
+    auto *window = static_cast<QXcbWindow *>(surface);
+    glXSwapBuffers(m_display, window->xcb_window());
+    if (window->needsSync())
+        window->postSyncWindowRequest();
+}
+
+QFunctionPointer QXcbXminContext::getProcAddress(const char *procName)
+{
+    return reinterpret_cast<QFunctionPointer>(
+        glXGetProcAddress(reinterpret_cast<const GLubyte *>(procName)));
+}
+
+QT_END_NAMESPACE
diff -Naur qtbase/src/plugins/platforms/xcb/gl_integrations/xcb_xmin/qxcbxmincontext.h Qt6_BLD/src/plugins/platforms/xcb/gl_integrations/xcb_xmin/qxcbxmincontext.h
--- qtbase/src/plugins/platforms/xcb/gl_integrations/xcb_xmin/qxcbxmincontext.h	1970-01-01 00:00:00.000000000 +0000
+++ Qt6_BLD/src/plugins/platforms/xcb/gl_integrations/xcb_xmin/qxcbxmincontext.h	2026-07-16 12:21:40.436777000 +0000
@@ -0,0 +1,38 @@
+// Copyright (C) 2026 Xmin Project
+// SPDX-License-Identifier: LicenseRef-Qt-Commercial OR LGPL-3.0-only OR GPL-2.0-only OR GPL-3.0-only
+
+#pragma once
+
+#include <qpa/qplatformopenglcontext.h>
+#include <QtGui/QSurfaceFormat>
+
+#include <GL/glx.h>
+
+QT_BEGIN_NAMESPACE
+
+class QOpenGLContext;
+class QXcbConnection;
+
+class QXcbXminContext : public QPlatformOpenGLContext
+{
+public:
+    QXcbXminContext(QXcbConnection *connection, QOpenGLContext *context);
+    ~QXcbXminContext() override;
+
+    bool makeCurrent(QPlatformSurface *surface) override;
+    void doneCurrent() override;
+    void swapBuffers(QPlatformSurface *surface) override;
+    QFunctionPointer getProcAddress(const char *procName) override;
+
+    QSurfaceFormat format() const override { return m_format; }
+    bool isSharing() const override { return m_shareContext != nullptr; }
+    bool isValid() const override { return m_context != nullptr; }
+
+private:
+    Display *m_display = nullptr;
+    GLXContext m_context = nullptr;
+    GLXContext m_shareContext = nullptr;
+    QSurfaceFormat m_format;
+};
+
+QT_END_NAMESPACE
diff -Naur qtbase/src/plugins/platforms/xcb/gl_integrations/xcb_xmin/qxcbxminintegration.cpp Qt6_BLD/src/plugins/platforms/xcb/gl_integrations/xcb_xmin/qxcbxminintegration.cpp
--- qtbase/src/plugins/platforms/xcb/gl_integrations/xcb_xmin/qxcbxminintegration.cpp	1970-01-01 00:00:00.000000000 +0000
+++ Qt6_BLD/src/plugins/platforms/xcb/gl_integrations/xcb_xmin/qxcbxminintegration.cpp	2026-07-16 12:21:40.437045000 +0000
@@ -0,0 +1,38 @@
+// Copyright (C) 2026 Xmin Project
+// SPDX-License-Identifier: LicenseRef-Qt-Commercial OR LGPL-3.0-only OR GPL-2.0-only OR GPL-3.0-only
+
+#include "qxcbxminintegration.h"
+
+#include "qxcbxmincontext.h"
+#include "qxcbconnection.h"
+#include "qxcbwindow.h"
+
+QT_BEGIN_NAMESPACE
+
+bool QXcbXminIntegration::initialize(QXcbConnection *connection)
+{
+    m_connection = connection;
+    qCDebug(lcQpaGl) << "Xmin software-direct xcb GL integration initialized";
+    return connection && connection->xcb_connection();
+}
+
+QXcbWindow *QXcbXminIntegration::createWindow(QWindow *window) const
+{
+    return new QXcbWindow(window);
+}
+
+#ifndef QT_NO_OPENGL
+QPlatformOpenGLContext *
+QXcbXminIntegration::createPlatformOpenGLContext(QOpenGLContext *context) const
+{
+    return new QXcbXminContext(m_connection, context);
+}
+#endif
+
+QPlatformOffscreenSurface *
+QXcbXminIntegration::createPlatformOffscreenSurface(QOffscreenSurface *) const
+{
+    return nullptr;
+}
+
+QT_END_NAMESPACE
diff -Naur qtbase/src/plugins/platforms/xcb/gl_integrations/xcb_xmin/qxcbxminintegration.h Qt6_BLD/src/plugins/platforms/xcb/gl_integrations/xcb_xmin/qxcbxminintegration.h
--- qtbase/src/plugins/platforms/xcb/gl_integrations/xcb_xmin/qxcbxminintegration.h	1970-01-01 00:00:00.000000000 +0000
+++ Qt6_BLD/src/plugins/platforms/xcb/gl_integrations/xcb_xmin/qxcbxminintegration.h	2026-07-16 12:21:40.436954000 +0000
@@ -0,0 +1,25 @@
+// Copyright (C) 2026 Xmin Project
+// SPDX-License-Identifier: LicenseRef-Qt-Commercial OR LGPL-3.0-only OR GPL-2.0-only OR GPL-3.0-only
+
+#pragma once
+
+#include "qxcbglintegration.h"
+
+QT_BEGIN_NAMESPACE
+
+class QXcbXminIntegration : public QXcbGlIntegration
+{
+public:
+    bool initialize(QXcbConnection *connection) override;
+    QXcbWindow *createWindow(QWindow *window) const override;
+#ifndef QT_NO_OPENGL
+    QPlatformOpenGLContext *createPlatformOpenGLContext(QOpenGLContext *context) const override;
+#endif
+    QPlatformOffscreenSurface *createPlatformOffscreenSurface(QOffscreenSurface *) const override;
+    bool supportsThreadedOpenGL() const override { return false; }
+
+private:
+    QXcbConnection *m_connection = nullptr;
+};
+
+QT_END_NAMESPACE
diff -Naur qtbase/src/plugins/platforms/xcb/gl_integrations/xcb_xmin/qxcbxminmain.cpp Qt6_BLD/src/plugins/platforms/xcb/gl_integrations/xcb_xmin/qxcbxminmain.cpp
--- qtbase/src/plugins/platforms/xcb/gl_integrations/xcb_xmin/qxcbxminmain.cpp	1970-01-01 00:00:00.000000000 +0000
+++ Qt6_BLD/src/plugins/platforms/xcb/gl_integrations/xcb_xmin/qxcbxminmain.cpp	2026-07-16 12:21:40.437127000 +0000
@@ -0,0 +1,22 @@
+// Copyright (C) 2026 Xmin Project
+// SPDX-License-Identifier: LicenseRef-Qt-Commercial OR LGPL-3.0-only OR GPL-2.0-only OR GPL-3.0-only
+
+#include "qxcbglintegrationplugin.h"
+#include "qxcbxminintegration.h"
+
+QT_BEGIN_NAMESPACE
+
+class QXcbXminIntegrationPlugin : public QXcbGlIntegrationPlugin
+{
+    Q_OBJECT
+    Q_PLUGIN_METADATA(IID QXcbGlIntegrationFactoryInterface_iid FILE "xcb_xmin.json")
+public:
+    QXcbGlIntegration *create() override
+    {
+        return new QXcbXminIntegration;
+    }
+};
+
+QT_END_NAMESPACE
+
+#include "qxcbxminmain.moc"
diff -Naur qtbase/src/plugins/platforms/xcb/gl_integrations/xcb_xmin/xcb_xmin.json Qt6_BLD/src/plugins/platforms/xcb/gl_integrations/xcb_xmin/xcb_xmin.json
--- qtbase/src/plugins/platforms/xcb/gl_integrations/xcb_xmin/xcb_xmin.json	1970-01-01 00:00:00.000000000 +0000
+++ Qt6_BLD/src/plugins/platforms/xcb/gl_integrations/xcb_xmin/xcb_xmin.json	2026-07-16 12:21:40.436700000 +0000
@@ -0,0 +1,3 @@
+{
+    "Keys": [ "xcb_xmin" ]
+}
diff -Naur qtbase/src/plugins/platforms/xcb/qxcbconnection.cpp Qt6_BLD/src/plugins/platforms/xcb/qxcbconnection.cpp
--- qtbase/src/plugins/platforms/xcb/qxcbconnection.cpp	2026-07-16 10:33:44.656701000 +0000
+++ Qt6_BLD/src/plugins/platforms/xcb/qxcbconnection.cpp	2026-07-16 12:21:40.436317000 +0000
@@ -1148,6 +1148,9 @@
         return m_glIntegration;
 
     QStringList glIntegrationNames;
+#ifdef QT_XCB_XMIN_OPENGL
+    glIntegrationNames << QStringLiteral("xcb_xmin");
+#endif
     glIntegrationNames << QStringLiteral("xcb_glx") << QStringLiteral("xcb_egl");
     QString glIntegrationName = QString::fromLocal8Bit(qgetenv("QT_XCB_GL_INTEGRATION"));
     if (!glIntegrationName.isEmpty()) {
