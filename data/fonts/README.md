# Embedded core fonts

This directory will hold generated, uncompressed C resources for the core X11
`fixed` and `cursor` fonts. Generated files must record their input release and
regeneration command.

