# Embedded keyboard maps

This directory will hold the generation recipe and committed C representation of
the default US PC105 XKB map. `xkbcomp` and `xkeyboard-config` are maintenance-time
inputs, not Xmin runtime dependencies.

