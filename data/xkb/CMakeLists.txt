# Generated C representation of the default keyboard map.  xkbcomp and the
# xkeyboard-config source tree are maintenance inputs, never build inputs.
xmin_add_component(xkb_data
  PUBLIC_INCLUDE_DIRS "${CMAKE_CURRENT_SOURCE_DIR}"
)
